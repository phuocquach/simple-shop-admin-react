import React from "react";

export const LogoutCallback = () => (
    // <AuthConsumer>
    //     {({ signoutRedirectCallback }) => {
    //         signoutRedirectCallback();
    //         return <span>loading</span>;
    //     }}
    // </AuthConsumer>
     <span>loading</span>
);